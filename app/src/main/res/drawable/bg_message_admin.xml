<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền của tin nhắn admin -->
    <solid android:color="#FFEB3B" />

    <!-- Bo tròn các góc -->
    <corners android:radius="12dp" />

    <!-- Đổ bóng hoặc padding nếu cần -->
    <padding
        android:left="12dp"
        android:top="8dp"
        android:right="12dp"
        android:bottom="8dp" />
</shape>
